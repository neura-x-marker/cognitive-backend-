"""
Tests package initialization.

Usage:
1. Create a new file for your tests (e.g., `api/tests/test_auth.py`).
2. Define your test cases inside that file.
3. Import them here if you want them to be explicitly discoverable at the package level,
   though Django's test runner usually finds `test_*.py` files automatically.
   
Example:
   from .test_auth import AuthTests
"""
