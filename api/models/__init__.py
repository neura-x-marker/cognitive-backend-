"""
Models package initialization.

Usage:
1. Create a new file for your model (e.g., `api/models/user_profile.py`).
2. Define your model class inside that file.
3. Import the model here to make it available at the package level:
   `from .user_profile import UserProfile`
"""
